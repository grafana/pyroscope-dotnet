name: Integration Test

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [main]

permissions:
  contents: read

jobs:
  build-profiler:
    uses: ./.github/workflows/build_linux_profiler.yml

  unit-test:
    name: dockertest unit tests (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        arch: ['x86_64', 'aarch64']
        include:
          - arch: x86_64
            runner: ${{ github.repository_owner == 'grafana' && 'ubuntu-x64-large' || 'ubuntu-latest' }}
          - arch: aarch64
            runner: ${{ github.repository_owner == 'grafana' && 'ubuntu-arm64-large' || 'ubuntu-24.04-arm' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version: 'stable'
          cache-dependency-path: integration-test/go.sum
      - name: Run dockertest unit tests
        run: go test -v -timeout 5m ./dockertest/
        working-directory: integration-test

  test:
    name: integration tests (${{ matrix.arch }}, ${{ matrix.libc_type }}, .NET ${{ matrix.version }})
    needs: build-profiler
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        arch: ['x86_64', 'aarch64']
        libc_type: [glibc, musl]
        version: ['8.0', '9.0', '10.0']
        include:
          - arch: x86_64
            runner: ${{ github.repository_owner == 'grafana' && 'ubuntu-x64-large' || 'ubuntu-latest' }}
          - arch: aarch64
            runner: ${{ github.repository_owner == 'grafana' && 'ubuntu-arm64-large' || 'ubuntu-24.04-arm' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: true
      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version: 'stable'
          cache-dependency-path: integration-test/go.sum
      - name: Download profiler binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: profiler-linux-${{ matrix.arch }}-${{ matrix.libc_type }}
          path: profiler-bin
      - name: Run integration test
        run: go test -v -timeout 90m -count=1 .
        env:
          LIBC_TYPE: ${{ matrix.libc_type }}
          DOTNET_VERSION: ${{ matrix.version }}
        working-directory: integration-test
